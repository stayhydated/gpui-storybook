group = "gpui-storybook-example-component"

# Optional launch-specific initial layout:
# window_mode = "dock"

# Optional launch-only presentation overrides:
# [overrides]
# color_scheme = "dark"
# theme = "Default Dark"
# language = "en"
