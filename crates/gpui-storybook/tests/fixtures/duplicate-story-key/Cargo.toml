[workspace]

[package]
name = "gpui-storybook-duplicate-fixture"
version = "0.0.0"
edition = "2024"
publish = false
rust-version = "1.98"

[dependencies]
gpui-kit = "0.6.0"
gpui-storybook = { path = "../../.." }

[patch.crates-io]
gpui-pre = { branch = "linux-headless-renderer", git = "https://github.com/stayhydated/zed" }
