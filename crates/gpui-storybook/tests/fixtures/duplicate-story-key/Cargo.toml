[workspace]

[package]
name = "gpui-storybook-duplicate-fixture"
version = "0.0.0"
edition = "2024"
publish = false
rust-version = "1.98"

[dependencies]
gpui-kit = "0.6.4"
gpui-storybook = { path = "../../.." }
